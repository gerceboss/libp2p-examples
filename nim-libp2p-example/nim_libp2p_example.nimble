version       = "0.1.0"
author        = "nim-libp2p example"
description   = "Relay + HTTP addresses example using nim-libp2p"
license       = "MIT"
srcDir        = "."
bin           = @["main"]

requires "nim >= 2.0.16"
requires "libp2p == 1.15.0"
requires "chronos"

